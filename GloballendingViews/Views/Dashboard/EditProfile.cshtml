@{
    ViewBag.Title = "Customer";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<br />



        <div class="forms">
            @if (TempData["message"] == null)
            {
                <div class=""></div>
            }
            else
            {
                <div id="Msg" class="alert alert-success">
                    <h4> @TempData["message"] </h4>
                </div>
            }
            <h2 class="title1"></h2>
            <div class="form-grids row widget-shadow" data-example-id="basic-forms">
                <div class="form-title">
                    <h4>Update Profile</h4>
                </div>
                <div class="row">
                    <div class="col-md-3"></div>
                    <div class="col-md-6">
                        <div class="form-body">
@using (Html.BeginForm("EditProfile", "Dashboard", FormMethod.Post, new { id = "signup" }))
{
                            <form>

@if (ViewBag.Data == null)
{
    <h2>No Record Found</h2>
}
else
{
    foreach (var Report in ViewBag.Data)
    {
        <div class="form-group">
            <input type="text" style="display:none;" name="id" class="form-control1" value="@Report.id" id="exampleInputEmail1" placeholder="ID">
       
              <label for="exampleInputEmail1">Firstname</label>
  <input type="text" name="firstname" class="form-control1" value="@Report.firstname" id="exampleInputEmail1" placeholder="Firstname">
                                </div>

                                <div class="form-group">
                                    <label for="exampleInputEmail1">Lastname</label>
                                    <input type="text" class="form-control1" value="@Report.lastname" name="lastname" id="exampleInputEmail1" placeholder="Lastname">
                                </div>

                                <div class="form-group">
                                    <label for="exampleInputEmail1">Email</label>
                                    <input type="text" class="form-control1" value="@Report.Email" name="Email" id="exampleInputEmail1" placeholder="Email">
                                </div>

                                <div class="form-group">
                                    <label for="exampleInputEmail1">Phone</label>
                                    <input type="text" name="Phone" class="form-control1" value="@Report.Phone" id="exampleInputEmail1" placeholder="Phone">
                                </div>


    }
        <button type="submit" class="btn btn-default">Update Profile</button>
}
                            </form>
                            }
                        </div>
                    </div>
                    <div class="col-md-3"></div>
                </div>
            </div>
        </div>
@*}
        <input type="submit" class="" value="Update Record" />
}
    </form>
    
}*@

